__pycache__/
*.pyc
.DS_Store

# Regenerated by `make` from raw/maps/*.w3x. Holds the imported .mdx/.blp art,
# which is third-party IP (see docs/04) and must not be redistributed from here.
# The JASS scripts, which are what you actually want to read, are copied to
# data/scripts/ instead.
assets/extracted/
