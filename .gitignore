__pycache__/
*.pyc
.DS_Store

node_modules/
android/
ios/
www/vendor/

# Clés de signature : jamais commitées, elles vivent dans les secrets GitHub
credentials/
credentials.json
*.jks
*.keystore

# Regénéré par `make` depuis raw/maps/*.w3x. Contient l'art importé (.mdx,
# .blp), qui est de la propriété intellectuelle tierce (voir docs/04) et ne
# doit pas être rediffusé d'ici. Les scripts JASS, eux — ce qu'on veut vraiment
# lire — sont recopiés dans data/scripts/.
assets/extracted/
